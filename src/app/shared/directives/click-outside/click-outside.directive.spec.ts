import { CloseOnClickoutDirective } from './click-outside.directive';

describe('CloseOnClickoutDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseOnClickoutDirective();
    expect(directive).toBeTruthy();
  });
});
